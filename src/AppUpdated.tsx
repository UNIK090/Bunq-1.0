import React from 'react';
import DashboardUpdated from './components/Dashboard/DashboardUpdated'; // Importing the updated dashboard

const AppUpdated: React.FC = () => {
  return (
    <div>
      <DashboardUpdated /> {/* Using the updated dashboard */}
      {/* Other components can be added here */}
    </div>
  );
};

export default AppUpdated;
